

const Layout = ({children}) => {
  return (
    <div>
        <nav>
            navbar
        </nav>

        <main>
            {children}
        </main>

        <footer>derechos reservados</footer>



    </div>
  )
}

export default Layout